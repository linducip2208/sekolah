# Flutter App — Setup & Development Guide

## Prerequisites

```bash
# Flutter 3.x (stable channel)
flutter --version  # harus >= 3.19.0

# Dart SDK (bundled dengan Flutter)
dart --version     # harus >= 3.3.0

# Tools
java --version     # JDK 17 untuk Android
xcode-select --version  # macOS only (iOS build)
```

---

## Project Structure

```
flutter_app/
├── android/
├── ios/
├── lib/
│   ├── main.dart
│   ├── firebase_options.dart          ← generated by FlutterFire CLI
│   ├── app/
│   │   ├── app.dart
│   │   ├── router/
│   │   │   ├── app_router.dart
│   │   │   └── routes.dart
│   │   └── theme/
│   │       ├── app_colors.dart
│   │       ├── app_theme.dart
│   │       └── app_typography.dart
│   ├── core/
│   │   ├── api/
│   │   │   ├── api_client.dart         ← Dio singleton + interceptors
│   │   │   ├── api_endpoints.dart
│   │   │   └── interceptors/
│   │   │       ├── auth_interceptor.dart
│   │   │       └── error_interceptor.dart
│   │   ├── storage/
│   │   │   └── app_storage.dart
│   │   ├── notifications/
│   │   │   ├── fcm_service.dart
│   │   │   └── notification_handler.dart
│   │   ├── websocket/
│   │   │   └── pusher_service.dart
│   │   └── utils/
│   │       ├── currency_formatter.dart
│   │       └── date_formatter.dart
│   ├── features/
│   │   ├── auth/
│   │   ├── dashboard/
│   │   ├── attendance/
│   │   ├── timetable/
│   │   ├── classroom/
│   │   ├── exam/
│   │   ├── marks/
│   │   ├── fees/
│   │   ├── library/
│   │   ├── hostel/
│   │   ├── transport/
│   │   ├── notice/
│   │   ├── chat/
│   │   ├── notifications/
│   │   └── profile/
│   └── shells/
│       ├── student_shell.dart
│       ├── parent_shell.dart
│       ├── teacher_shell.dart
│       ├── admin_shell.dart
│       └── staff_shell.dart
├── assets/
│   ├── images/
│   ├── icons/
│   └── l10n/
│       ├── app_id.arb                 ← Bahasa Indonesia
│       ├── app_en.arb                 ← English
│       └── app_ar.arb                 ← Arabic (RTL)
├── test/
└── pubspec.yaml
```

---

## pubspec.yaml

```yaml
name: eschool_app
description: eSchool SaaS — Multi-tenant School Management

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  bloc: ^8.1.4

  # Navigation
  go_router: ^13.2.1

  # HTTP
  dio: ^5.4.3
  pretty_dio_logger: ^1.3.1

  # Storage
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3

  # Firebase
  firebase_core: ^2.30.1
  firebase_messaging: ^14.9.4

  # Real-time Chat
  pusher_channels_flutter: ^2.1.0

  # UI
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  fl_chart: ^0.66.2
  lottie: ^3.1.0
  flutter_svg: ^2.0.10
  intl: ^0.19.0

  # Files & Media
  file_picker: ^8.0.7
  flutter_pdfview: ^1.3.2
  image_picker: ^1.1.2
  path_provider: ^2.1.3

  # Scanner (librarian)
  mobile_scanner: ^3.5.7

  # Utils
  timeago: ^3.7.0
  url_launcher: ^6.3.0
  connectivity_plus: ^6.0.3
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.7
  mocktail: ^1.0.3
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true    # untuk l10n

  assets:
    - assets/images/
    - assets/icons/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
```

---

## Setup Firebase

```bash
# Install FlutterFire CLI
dart pub global activate flutterfire_cli

# Login ke Firebase
firebase login

# Configure (jalankan di root flutter_app/)
flutterfire configure \
  --project=eschool-saas-prod \
  --platforms=android,ios

# Ini akan generate:
# - android/app/google-services.json
# - ios/Runner/GoogleService-Info.plist
# - lib/firebase_options.dart
```

---

## Environment Setup

```dart
// lib/core/config/app_config.dart
class AppConfig {
  static const String apiBaseUrl = String.fromEnvironment(
    'API_BASE_URL',
    defaultValue: 'http://127.0.0.1:8000/api/v1',
  );

  static const String pusherKey = String.fromEnvironment(
    'PUSHER_KEY',
    defaultValue: 'local-key',
  );

  static const String pusherHost = String.fromEnvironment(
    'PUSHER_HOST',
    defaultValue: '127.0.0.1',
  );

  static const int pusherPort = int.fromEnvironment(
    'PUSHER_PORT',
    defaultValue: 6001,
  );
}
```

---

## Menjalankan App

```bash
# Development — Laravel lokal
flutter run \
  --dart-define=API_BASE_URL=http://10.0.2.2:8000/api/v1 \
  --dart-define=PUSHER_KEY=local \
  --dart-define=PUSHER_HOST=10.0.2.2 \
  --dart-define=PUSHER_PORT=6001

# Development — device fisik (ganti IP dengan IP komputer)
flutter run \
  --dart-define=API_BASE_URL=http://192.168.1.100:8000/api/v1

# Staging
flutter run --dart-define=API_BASE_URL=https://staging.eschool.app/api/v1

# Production build
flutter build apk --release \
  --dart-define=API_BASE_URL=https://api.eschool.app/api/v1 \
  --dart-define=PUSHER_KEY=prod-key \
  --dart-define=PUSHER_HOST=api.eschool.app

flutter build ipa --release \
  --dart-define=API_BASE_URL=https://api.eschool.app/api/v1
```

---

## API Client Setup

```dart
// lib/core/api/api_client.dart
class ApiClient {
  static Dio? _instance;

  static Dio get instance {
    _instance ??= Dio(BaseOptions(
      baseUrl: AppConfig.apiBaseUrl,
      connectTimeout: const Duration(seconds: 15),
      receiveTimeout: const Duration(seconds: 30),
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
      },
    ))
      ..interceptors.addAll([
        AuthInterceptor(),
        ErrorInterceptor(),
        if (kDebugMode) PrettyDioLogger(requestBody: true, responseBody: true),
      ]);
    return _instance!;
  }
}
```

```dart
// lib/core/api/interceptors/auth_interceptor.dart
class AuthInterceptor extends Interceptor {
  @override
  void onRequest(RequestOptions options, RequestInterceptorHandler handler) async {
    final token = await AppStorage.getToken();
    if (token != null) {
      options.headers['Authorization'] = 'Bearer $token';
    }
    handler.next(options);
  }

  @override
  void onError(DioException err, ErrorInterceptorHandler handler) async {
    if (err.response?.statusCode == 401) {
      // Token expired — logout
      await AppStorage.clear();
      AppRouter.instance.go('/login');
    }
    handler.next(err);
  }
}
```

---

## Pusher Real-time Chat Setup

```dart
// lib/core/websocket/pusher_service.dart
class PusherService {
  static PusherChannelsFlutter? _pusher;

  static Future<void> init(String authToken) async {
    _pusher = PusherChannelsFlutter.getInstance();
    await _pusher!.init(
      apiKey: AppConfig.pusherKey,
      cluster: 'ap1',
      wsHost: AppConfig.pusherHost,
      wsPort: AppConfig.pusherPort,
      authEndpoint: '${AppConfig.apiBaseUrl}/broadcasting/auth',
      authParams: {
        'headers': {'Authorization': 'Bearer $authToken'},
      },
    );
    await _pusher!.connect();
  }

  static Future<void> subscribeToConversation(
    int conversationId,
    Function(PusherEvent) onMessage,
  ) async {
    await _pusher!.subscribe(
      channelName: 'private-conversation.$conversationId',
      onEvent: onMessage,
    );
  }

  static Future<void> disconnect() async {
    await _pusher?.disconnect();
  }
}
```

---

## Testing

```bash
# Unit tests
flutter test test/unit/

# Widget tests
flutter test test/widget/

# Integration tests (emulator harus running)
flutter test integration_test/

# Coverage
flutter test --coverage
genhtml coverage/lcov.info -o coverage/html
```

---

## Build & Release

```bash
# Android — keystore setup (sekali)
keytool -genkey -v -keystore android/eschool-release.jks \
  -alias eschool -keyalg RSA -keysize 2048 -validity 10000

# Tambahkan ke android/key.properties:
# storePassword=...
# keyPassword=...
# keyAlias=eschool
# storeFile=../eschool-release.jks

# Build APK
flutter build apk --release

# Build App Bundle (untuk Google Play)
flutter build appbundle --release

# iOS (macOS only)
flutter build ipa --release
```
